@robot 'initbot'

fs = require 'fs'

data = fs.readFileSync(__dirname + '/init.json').toString().split('\n')
console.log data
data = (JSON.parse line for line in data when line != '')

@sendRaw msg for msg in data

