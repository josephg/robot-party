@robot "nix"

user = undefined

@listen 'usercmd', ({data:cmd}, reply) ->
  if match = cmd.match /nick\s+(.*)$/
    olduser = user
    user = match[1]
    @transmit "user nickname", user, user: olduser
    @transmit "user nickname", user, local: true, user: olduser